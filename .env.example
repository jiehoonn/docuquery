# =============================================================================
# DocuQuery Environment Variables
#
# Quick start:
#   1. Copy this file:  cp .env.example .env
#   2. Add your Gemini API key (free at https://aistudio.google.com/apikeys)
#   3. Run: docker compose up --build
#   4. Open: http://localhost:3000
# =============================================================================

# ---- Required: PostgreSQL (used by Docker to create the database) ----
POSTGRES_USER=docuquery
POSTGRES_PASSWORD=docuquery
POSTGRES_DB=docuquery

# ---- Required: Database connection string (must match Postgres vars above) ----
DATABASE_URL=postgresql+asyncpg://docuquery:docuquery@localhost:5432/docuquery

# ---- Required: Gemini API key (the only external key you need) ----
# Get a free key at: https://aistudio.google.com/apikeys
GEMINI_API_KEY=your_gemini_api_key_here

# ---- Optional: Override defaults if needed ----
# REDIS_URL=redis://localhost:6379        # Default works for Docker setup
# QDRANT_HOST=localhost                   # Default works for Docker setup
# QDRANT_PORT=6333                        # Default works for Docker setup
# JWT_SECRET=dev-secret-change-in-production  # Change in production
# JWT_ALGORITHM=HS256                     # Default is fine